---
title: "Belvedir Documentation"
sidebarTitle: "Introduction"
description: "Instrument your LLM agents, collect traces, and improve them automatically with Belvedir."
---

Belvedir is an observability and auto-improvement platform for LLM agents. Install the SDK, wrap your agent runs in sessions, and Belvedir segments the traces into tasks, groups similar work together, and runs improvement loops (harness evolution, prompt evolution, and LoRA fine-tuning) against your production traffic.

## Where to start

- [Quickstart](/quickstart): get traces flowing in under 5 minutes with the Node SDK.
- [Python SDK](/sdk/python): same concepts, context managers instead of callbacks.
- [How It Works](/how-it-works): the six-stage pipeline (instrument, collect, segment, group, clean, optimize).
- [Importing Traces](/importing-traces): bring history over from LangSmith, Langfuse, Phoenix, Braintrust, or a file.

## What you can do

- **Trace** every LLM and tool call from OpenAI, Anthropic, Cohere, Bedrock, Vertex AI, and more.
- **Segment** sessions into tasks automatically and gather similar tasks into groups.
- **Optimize** with [harness evolution and prompt evolution](/training/optimization-loops) (GEPA) that open PRs against your GitHub repo, and [LoRA fine-tuning](/training/overview) on your traces.
- **Benchmark** any agent harness in an isolated CPU or GPU sandbox against SWE-bench, τ-bench, GAIA, and more.
- **Train** open models on your successful production traffic and download the LoRA adapter.
- **Route** each call to the model that fits it with [Model Routing](/inference/mixture-of-models).

## Supported runtimes

The Node SDK (`@belvedir/loop`) and Python SDK (`belvedir-loop`) both auto-instrument the major LLM providers. (The legacy `@fractalresearch/loop` and `fractal-loop` packages still work but are deprecated.) Local models via [Ollama](/inference/local-models) and hosted OpenAI-compatible endpoints ([Together](https://together.ai), [Fireworks](https://fireworks.ai), [Groq](https://groq.com), [OpenRouter](https://openrouter.ai), [vLLM](https://docs.vllm.ai)) work out of the box. See [Hosted Inference](/inference/hosted).

<Tip>
Working with a coding agent? The whole integration guide lives at [platform.belvedir.ai/agents.md](https://platform.belvedir.ai/agents.md) in agent-readable markdown: both SDKs, outcome reporting, and the common pitfalls. The dashboard's Get Started page has a **Copy prompt** button that puts a ready-made instruction on your clipboard. Paste it into your coding agent and it fetches that page and instruments the app for you. It also works as a skill file: save it as `.claude/skills/belvedir/SKILL.md`.
</Tip>
